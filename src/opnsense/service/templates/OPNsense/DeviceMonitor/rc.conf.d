# The daemon runs whenever the plugin is installed. It re-reads config.json on
# every loop and only performs scans while "enabled" is set, so the rcvar is
# not tied to the enabled setting.
devicemonitor_enable="YES"
